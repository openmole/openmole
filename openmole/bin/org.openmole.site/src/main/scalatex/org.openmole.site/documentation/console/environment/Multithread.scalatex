
@import org.openmole.site.Objects._

@sect{Multi-threaded environment}
  By default OpenMOLE tasks run on a single thread. To use several cores of a computer you can define a so called local environment and parameter the number of threads.
  @br @hl.openmole("""
  // For using 10 threads / cores
  val env = LocalEnvironment(10)""")