package org.openmole.core.workflow

/**
 * The package domain provides abstract properties for domains ( finite, discrete, bounded, ...)
 * ; the domains are then implemented through implicit conversions in plugins.
 *
 * For example, the [[org.openmole.plugin.domain.range.Range]] implements [[domain.Bounds]] and [[domain.Center]]
 *
 */
package object domain
